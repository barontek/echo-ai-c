/*
 * agent_run.h - the agent run loop contracts. The full public API docs
 * live in agent.h; this header exists for the module's exports.
 * Depends on: agent.h.
 */

#ifndef ECHO_AGENT_RUN_H
#define ECHO_AGENT_RUN_H

#include "agent.h"

/* agent_run_new, agent_run_streaming_new, agent_run_streaming_context_new
 * are defined in this unit and declared (with full docs) in agent.h. */

#endif /* ECHO_AGENT_RUN_H */
