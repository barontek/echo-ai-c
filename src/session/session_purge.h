/*
 * session_purge.h - session deletion and purge contracts. The public
 * API is declared in session_manager.h; this header exists for the
 * module's internal helpers.
 * Depends on: session_manager.h.
 */

#ifndef ECHO_SESSION_PURGE_H
#define ECHO_SESSION_PURGE_H

#include "session_manager.h"

/* No internal cross-unit symbols: session_purge.c is self-contained. */

#endif /* ECHO_SESSION_PURGE_H */
