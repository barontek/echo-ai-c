/*
 * session_list.h - session listing contracts. The public list API is
 * declared in session_manager.h; this header exists for the module's
 * internal row helpers.
 * Depends on: session_manager.h.
 */

#ifndef ECHO_SESSION_LIST_H
#define ECHO_SESSION_LIST_H

#include "session_manager.h"

/* No internal cross-unit symbols: session_list.c is self-contained. */

#endif /* ECHO_SESSION_LIST_H */
